package server

import (
	//"log"
	//"net/http"
	"main/config"
)

func Start(app *config.Application) {
	//mux := http.NewServeMux()
	//mux.HandleFunc("/", home)
	//mux.HandleFunc("/post/view", postview)
	//mux.HandleFunc("/query/create", getPosts)

	//log.Print(starting)
}
